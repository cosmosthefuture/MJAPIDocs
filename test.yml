info:
  name: test
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/test"
  headers:
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
