info:
  name: delete
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/masters/agents/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{masterAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
