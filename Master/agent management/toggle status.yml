info:
  name: toggle status
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/masters/agents/:id/toggle-status"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "3"
      type: path
  auth:
    type: bearer
    token: "{{masterAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
