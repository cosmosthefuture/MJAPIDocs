info:
  name: update
  type: http
  seq: 11

http:
  method: PUT
  url: "{{baseUrl}}/masters/users/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "User Three dfdsf",
        "email": "userthrefde@example.com", 
        "phone_number": "09333333333",
        "username": "userthrdfsdfee123",
        "agent_code": "AGENT"
      }
  auth:
    type: bearer
    token: "{{masterAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
