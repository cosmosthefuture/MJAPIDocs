info:
  name: balance
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/agents/wallet-balance"
  headers:
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{agentAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
