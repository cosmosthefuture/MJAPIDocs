info:
  name: test ws health
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:3000/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
