info:
  name: delete ws state
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/health/state/clear/:roomId
  params:
    - name: roomId
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
