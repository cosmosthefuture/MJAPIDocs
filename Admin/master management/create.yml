info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/admins/masters"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Master Zaw Gyi",
      //   "email": "kyawgyi@example.com",
        "phone_number": "09222222222",
        "username": "zawgyiexample123",
        "winning_commission_percentage": 5,
        "password": "password123",
        "password_confirmation": "password123"
      }
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
