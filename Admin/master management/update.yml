info:
  name: update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/admins/masters/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Master Zaw Gyi update",
      //   "email": "kyawgyi@example.com",
        "phone_number": "09111111111",
        "username": "zawgyiupdateexample123",
        "winning_commission_percentage": 7,
        "password": "password123456",
        "password_confirmation": "password123456"
      }
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
