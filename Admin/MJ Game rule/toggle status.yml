info:
  name: toggle status
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/admins/mah-jong-game-rules/:id/toggle-status"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "1"
      type: path
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
