info:
  name: delete
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/admins/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "7"
      type: path
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
