info:
  name: update
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseUrl}}/admins/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "Update Admin",
      //   "email": "updateadmin@example.com",
        "phone_number": "094551456",
        "username": "updateadmin123",
        "permission_type_ids": [1,2,3,4]
      }
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
