info:
  name: update
  type: http
  seq: 7

http:
  method: PUT
  url: "{{baseUrl}}/admins/users/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "name": "User One update",
        "email": "useroneupdate@example.com", 
        "phone_number": "09111111111",
        "username": "userone123",
        "agent_code": "AGENT"
      }
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
