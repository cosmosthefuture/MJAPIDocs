info:
  name: update
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/admins/mah-jong-game-rooms/:id"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "room_name": "Room One",
        "room_code": "0003",
        "game_id": 1,
        "mah_jong_game_rule_id": 1
      }
  auth:
    type: bearer
    token: "{{adminAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
