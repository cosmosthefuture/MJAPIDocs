info:
  name: balance
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/users/wallet-balance"
  headers:
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{userAccessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
